# Clean Architecture - Visual Flow

## Login Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                │
│                    (React/Vite App)                             │
│                                                                 │
│   User enters username + password → Click Login                │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ POST /api/login
                      │ { username, password }
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│               PRESENTATION LAYER                                │
│          (ApiLoginServlet.java)                                 │
│                                                                 │
│  1. Parse HTTP request                                          │
│  2. Extract username & password from JSON                       │
│  3. Create LoginRequest DTO                                     │
│  4. Call loginUseCase.execute(request)                          │
│  5. Convert response to HTTP status + JSON                      │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ LoginRequest DTO
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│              APPLICATION LAYER                                  │
│           (LoginUseCase.java)                                   │
│                                                                 │
│  BUSINESS LOGIC:                                                │
│  1. Validate: username not blank                                │
│  2. Validate: password not blank                                │
│  3. Call userDao.isValidUser(username, password)                │
│  4. Return ApiResponse (success or error)                       │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ Call DAO
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                DATA ACCESS LAYER                                │
│              (UserDao.java)                                     │
│                                                                 │
│  1. Query database: SELECT password_hash WHERE user_id = ?      │
│  2. BCrypt compare: plaintext vs stored hash                    │
│  3. Return true/false                                           │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ SQL Query
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE                                   │
│                  (MySQL/MariaDB)                                │
│                                                                 │
│              users table                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## Register Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                │
│                    (React/Vite App)                             │
│                                                                 │
│   User fills form → Click Register                              │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ POST /api/register
                      │ { user_id, full_name, email, 
                      │   contact_number, password }
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│               PRESENTATION LAYER                                │
│         (ApiRegisterServlet.java)                               │
│                                                                 │
│  1. Parse HTTP request                                          │
│  2. Extract all fields from JSON                                │
│  3. Create RegisterRequest DTO                                  │
│  4. Call registerUseCase.execute(request)                       │
│  5. Convert response to HTTP status + JSON                      │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ RegisterRequest DTO
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│              APPLICATION LAYER                                  │
│          (RegisterUseCase.java)                                 │
│                                                                 │
│  BUSINESS LOGIC:                                                │
│  1. Validate: all required fields present                       │
│  2. Validate: user_id doesn't exist                             │
│  3. Validate: email doesn't exist                               │
│  4. Call userDao.registerUser(...)                              │
│  5. Return ApiResponse (success or error)                       │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ Call DAO
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                DATA ACCESS LAYER                                │
│              (UserDao.java)                                     │
│                                                                 │
│  1. Hash password using BCrypt                                  │
│  2. INSERT INTO users (...) VALUES (...)                        │
│  3. Set role_id = 3 (Customer)                                  │
│  4. Return true/false                                           │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ SQL Insert
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE                                   │
│                  (MySQL/MariaDB)                                │
│                                                                 │
│              users table                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## Layer Responsibilities

### 🎨 Presentation Layer (Servlets)
- Handle HTTP requests/responses
- Parse JSON
- Set status codes
- CORS headers
- Session management
- **NO business logic**

### 💼 Application Layer (Use Cases)
- Business logic & validation
- Orchestrate operations
- Use DTOs for data transfer
- Independent of HTTP
- **Testable without web server**

### 🗄️ Data Access Layer (DAO)
- Database queries
- Password hashing
- Connection management
- Return simple types (boolean, objects)

### 📦 DTOs (Data Transfer Objects)
- Simple POJOs
- No business logic
- Transfer data between layers
- Serialize to/from JSON

---

## Benefits of This Structure

✅ **Single Responsibility**: Each class has ONE job
✅ **Testable**: Test use cases without HTTP
✅ **Maintainable**: Easy to find where logic lives
✅ **Reusable**: Use cases work anywhere
✅ **Flexible**: Easy to change database or framework
✅ **Clean**: Clear separation of concerns

